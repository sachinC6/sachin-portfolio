/* GSAP 3.12.2 - Placeholder for local development */
/* When deployed to GitHub Pages, CDN version will be used */
/* This is a minimal fallback to prevent errors during local testing */
if (typeof gsap === 'undefined') {
    console.warn('GSAP CDN blocked - using fallback. Full animations will work when deployed.');
    window.gsap = {
        registerPlugin: function() {},
        timeline: function() { return { to: function() { return this; }, from: function() { return this; }, fromTo: function() { return this; } }; },
        to: function() {},
        from: function() {},
        fromTo: function() {},
        set: function() {},
        utils: { toArray: function(x) { return Array.from(document.querySelectorAll(x)); }, snap: function(x) { return function(v) { return Math.round(v/x)*x; }; } }
    };
    window.ScrollTrigger = { create: function() {} };
    window.Draggable = { create: function() { return [{}]; } };
}
